/**
 *  Test cases
 *  ==========
 *
 *  1. System booting: XML data transformed into JSON in database
 *  2. News category updates
 */

const assert = require('assert')

describe('Bootstrap', _ => {
  it('should return true', _ => {
    assert.equal(1, 1)
  })
})
